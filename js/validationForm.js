// test
const submit = document.querySelectorAll(".button");

submit.forEach((validateBtn) => validateBtn.addEventListener("click", validate));

function validate() {
        alert('Formulaire envoyé !');

    }
    